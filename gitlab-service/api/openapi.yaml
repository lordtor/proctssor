openapi: 3.0.3
info:
  title: GitLab Service API
  description: |
    GitLab integration service for workflow automation.
    
    This service provides integration with GitLab API for workflow automation,
    supporting merge requests, issues, pipelines, and commit status operations.
  version: 1.0.0
  contact:
    name: Workflow Engine Team

servers:
  - url: http://localhost:8081
    description: Development server
  - url: http://gitlab-service:8081
    description: Docker server

tags:
  - name: Health
    description: Health check endpoints
  - name: Service
    description: Service information

paths:
  /health:
    get:
      summary: Health check
      description: |
        Returns the health status of the service.
        Checks NATS and GitLab connectivity.
      operationId: getHealth
      tags:
        - Health
      responses:
        '200':
          description: Service is healthy
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
        '503':
          description: Service is unhealthy
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'

  /ready:
    get:
      summary: Readiness check
      description: |
        Returns whether the service is ready to accept traffic.
        Service is ready when NATS and GitLab are connected.
      operationId: getReady
      tags:
        - Health
      responses:
        '200':
          description: Service is ready
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReadyResponse'
        '503':
          description: Service is not ready

  /live:
    get:
      summary: Liveness check
      description: |
        Returns whether the service is alive.
        Used by Kubernetes for liveness probes.
      operationId: getLive
      tags:
        - Health
      responses:
        '200':
          description: Service is alive

  /info:
    get:
      summary: Service information
      description: Returns information about the service
      operationId: getInfo
      tags:
        - Service
      responses:
        '200':
          description: Service information
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ServiceInfo'

components:
  schemas:
    HealthResponse:
      type: object
      properties:
        status:
          type: string
          enum: [healthy, unhealthy]
          description: Overall health status
        service_name:
          type: string
          description: Service name
        timestamp:
          type: string
          format: date-time
          description: Current timestamp
        uptime_seconds:
          type: integer
          description: Service uptime in seconds
        checks:
          type: object
          additionalProperties:
            type: boolean
          description: Individual component health checks
      required:
        - status
        - service_name
        - timestamp

    ReadyResponse:
      type: object
      properties:
        status:
          type: string
          enum: [ready, not ready]
      required:
        - status

    ServiceInfo:
      type: object
      properties:
        name:
          type: string
        version:
          type: string
        description:
          type: string
        endpoints:
          type: array
          items:
            type: string
      required:
        - name
        - version
